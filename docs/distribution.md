# Distribution

Kyma publishes platform archives and the VS Code extension from tagged releases.

## CLI archives

Release archives are generated by CPack on clean GitHub Actions runners:

- `kyma-<version>-Linux-<arch>.tar.gz` for Linux
- `kyma-<version>-Windows-<arch>.zip` for Windows
- `kyma-<version>-Darwin-<arch>.tar.gz` and `.zip` for macOS

Each archive contains the `kyma` executable, `README.md`, `LICENSE`, the language documentation, and runnable `.ky` examples. Extract the archive and put its `bin` directory on `PATH` (or invoke the executable directly). The installed documentation and examples live below `share/kyma`.

## VS Code

The tagged release also contains `kyma-language-support-<version>.vsix`. Install it with:

```sh
code --install-extension kyma-language-support-0.2.1.vsix --force
```

The VSIX includes the purple Kyma icon, light/dark file and command icons, syntax grammar, snippets, completion rules, examples, and the language/diagnostics/standard-library documentation. The black-and-white branding asset remains included as an alternate asset.

Marketplace publication is enabled when the repository has a `VSCE_PAT` secret for the `kyma-lang` publisher. Without that secret, the GitHub release and VSIX remain installable directly.
